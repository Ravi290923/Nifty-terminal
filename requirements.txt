streamlit>=1.37
plotly>=5.22
pandas>=2.2
numpy>=1.26
requests>=2.32
python-dotenv>=1.0

# Only needed for true tick-by-tick streaming (sidebar toggle). REST-based
# candles/quotes work without it.
upstox-python-sdk>=2.15
